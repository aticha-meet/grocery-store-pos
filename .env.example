DATABASE_URL="file:./store.db"
PORT=3001
hostname=localhost
# Public backend URL (no /api path), when using Cloudflare Tunnel
# PD_HOSTNAME="https://grocery-pos.edflow.online"
# Optional additional frontend origins, comma separated
# CORS_ORIGINS="https://your-frontend.example"
